1. Question 1
Select the option that correctly completes the sentence:

Training a model using labeled data and using this model to predict the labels for new data is known as ____________.
a.Supervised Learning-a
b.Density Estimation
c.Unsupervised Learning
d.Clustering

2. Question 2
Select the option that correctly completes the sentence:

Modeling the features of an unlabeled dataset to find hidden structure is known as ____________.
a.Supervised Learning
b.Classification
c.Unsupervised Learning-a
d.Regression

3. Question 3
Select the option that correctly completes the sentence:

Training a model using categorically labelled data to predict labels for new data is known as __________.
a.Clustering
b.Classification-a
c.Feature Extraction
d.Regression

4. Question 4
Select the option that correctly completes the sentence:

Training a model using labelled data where the labels are continuous quantities to predict labels for new data is known as __________.
a.Clustering
b.Feature Extraction
c.Classification
d.Regression-a

6. Question 6
Which of the following is true for the nearest neighbor classifier (Select all that apply):
a.Memorizes the entire training set-a
b.Partitions observations into k clusters where each observation belongs to the cluster with the nearest mean
c.A higher value of k leads to a more complex decision boundary
d.Given a data instance to classify, computes the probability of each possible class using a statistical model of the input features

7. Question 7
Why is it important to examine your dataset as a first step in applying machine learning? (Select all that apply):
a.See what type of cleaning or preprocessing still needs to be done-a
b.You might notice missing data-a
c.Gain insight on what machine learning model might be appropriate, if any-a
d.Get a sense for how difficult the problem might be-a
e.It is not important

8. Question 8
The key purpose of splitting the dataset into training and test sets is:
a.To reduce the number of features we need to consider as input to the learning algorithm
b.To reduce the amount of labelled data needed for evaluating classifier accuracy
c.To estimate how well the learned model will generalize to new data-a
d.To speed up the training process

9. Question 9
The purpose of setting the random_state parameter in train_test_split is: (Select all that apply)
a.To avoid predictable splitting of the data
b.To make experiments easily reproducible by always using the same partitioning of the data-a
c.To split the data into similar subsets so that bias is not introduced into the final results
d.To avoid bias in data splitting

10. Question 10
Given a dataset with 10,000 observations and 50 features plus one 
label, what would be the dimensions of X_train, y_train, X_test, and y_test? 
Assume a train/test split of 75%/25%.
ans: X_train(7500,50)
     y_train(7500,)
     X_test(2500,50)
     y_test(2500,)
